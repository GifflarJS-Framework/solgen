default:
  image: node:10.15.3

stages:
  - test

quality:
  stage: test
  script:
    - npm install
    - npm test
    
pages:
  stage: test
  script:
    - mkdir .public
    - cp -r * .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - master
